#include "main.h"

/**
 * _isdigit - check if a character is a digit
 *@c: the number to be checked
 * Return: 1 for the character that will be a dgt or 0 for else
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

